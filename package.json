{
  "name": "omarchy-foundry-entry",
  "version": "0.1.0",
  "private": true,
  "description": "Proof-first local Omarchy plugin scaffold",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
